# from django.urls import path

# from . import views

app_name = "linkrot"
urlpatterns = [
    # path("<slug:ring>/linkrot", views.WebringListView.as_view(), name="check_all"),
    # path("<slug:ring>/link/<uuid:entry>", views.EntryView.as_view(), name="check_one"),
    # path("<slug:ring>/link/<uuid:entry>/history", views.EntryView.as_view(), name="history"),
]
